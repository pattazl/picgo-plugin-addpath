{
  "name": "picgo-plugin-addpath",
  "version": "1.0.0",
  "description": "A PicGo plugin for add path by cli",
  "main": "dist/index.js",
  "publishConfig": {
    "access": "public"
  },
  "homepage": "https://github.com/pattazl/picgo-plugin-addpath",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc -p .",
    "dev": "tsc -w -p .",
    "patch": "npm version patch && git push origin master && git push origin --tags",
    "minor": "npm version minor && git push origin master && git push origin --tags",
    "major": "npm version major && git push origin master && git push origin --tags"
  },
  "keywords": [
    "picgo",
    "picgo-gui-plugin",
    "picgo-plugin"
  ],
  "author": "AustinYoung",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^10.10.1",
    "picgo": "^1.2.0",
    "typescript": "^3.0.3"
  },
  "dependencies": {
  }
}
